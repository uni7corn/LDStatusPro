FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
RUN pip install --no-cache-dir .

COPY . .

RUN mkdir -p /app/data

EXPOSE 8000

CMD ["sh", "-c", "python scripts/migrate_schema.py && uvicorn app.main:app --host 0.0.0.0 --port 8000"]
