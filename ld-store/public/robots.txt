User-agent: *
Allow: /

# noindex is enforced by HTML metadata and X-Robots-Tag response headers.
# Crawlers must be allowed to fetch pages so they can observe that policy and
# so social/IM link unfurlers can continue to generate share previews.
